# 統合により不要になったMEXファイル

## 概要

以下のMEXファイルは、他のMEXファイルに統合されたため、実質的に不要になりました。ただし、互換性のため、現時点では削除せずに残すことを推奨します。

## 不要になったファイル

### 1. `mex_eskf_update_postprocess.cpp`
- **統合先**: `mex_eskf_do_update.cpp`
- **統合日**: 2024-12-29
- **状態**: `mex_eskf_do_update.cpp`に`handle_postprocess`関数を統合済み
- **削除可否**: 可能（ただし、他のコードから直接呼び出されている可能性があるため、互換性のため残すことを推奨）

### 2. `mex_eskf_predict_postprocess.cpp`
- **統合先**: `mex_run_eskf.cpp`
- **統合日**: 2024-12-29
- **状態**: `mex_run_eskf.cpp`の`call_predict`関数内に`handle_postprocess`ロジックを統合済み
- **削除可否**: 可能（ただし、他のコードから直接呼び出されている可能性があるため、互換性のため残すことを推奨）

## 削除前の確認事項

1. **他のコードからの呼び出し確認**
   - MATLABコードや他のMEXファイルから直接呼び出されていないか確認
   - `grep -r "mex_eskf_update_postprocess"` で検索
   - `grep -r "mex_eskf_predict_postprocess"` で検索

2. **ビルド設定の更新**
   - `build_mex.m`から該当ファイルのビルド設定を削除
   - ただし、現時点ではビルド設定は残しており、必要に応じてビルド可能

3. **テストの実行**
   - 統合後のコードが正しく動作することを確認
   - 既存のテストケースがすべて通過することを確認

## 推奨事項

現時点では、以下の理由により、これらのファイルを削除せずに残すことを推奨します：

1. **互換性**: 他のコードから直接呼び出されている可能性がある
2. **デバッグ**: 問題が発生した場合、元のファイルを参照できる
3. **段階的移行**: 完全に動作確認が取れるまで、元のファイルを残す

## 削除手順（将来）

1. 他のコードからの呼び出しを確認
2. すべてのテストが通過することを確認
3. `build_mex.m`からビルド設定を削除（またはコメントアウト）
4. ファイルを削除
5. ドキュメントを更新

