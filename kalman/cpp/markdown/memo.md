現在はセンサーの値を常に更新して、センサー周期に合わせて読み取る仕組みになっているが、
実際のセンサーはセンサー周期の間では同じ値が変えてくる
例えば、100Hzのセンサーを400Hzで読み取ると4回同じ値が帰ってくる
そうなるように、GenerateDataを修正してほしい
ESKFの方では、同じ値が来たら推定をスキップするようにして、対応してほしい
センサー周期を決定するのはGenerateDataのConfigであり、ESKFの方ではそのような変数は持たないで、値が変化する時を更新とする
